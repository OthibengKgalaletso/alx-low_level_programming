#include "main.h"

/**
 * _abs - check main
 * This function outputs the absolute value of integer
 * @r: integer to check
 * Return: the absolute value of integer r
 */


int _abs(int r)
{
	if (r >= 0)
	return (r);
	else
		return (r * -1);
}
